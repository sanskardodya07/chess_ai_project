{
  "version": 2,
  "builds": [
    {
      "src": "server/app.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "server/api/(.*)",
      "dest": "server/app.py"
    }
  ]
}